import {getSize} from '@utils/responsive';
import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  linearGradient: {
    height: getSize.s(500),
    borderRadius: getSize.m(6),
  },
});

export default styles;
